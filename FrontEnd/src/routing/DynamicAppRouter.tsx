import { BrowserRouter, Routes, Route } from "react-router-dom"
import DynamicPageLoader from "./DynamicPageLoader"

export default function DynamicAppRouter() {
  return (
    <BrowserRouter>
      <Routes>
      </Routes>
    </BrowserRouter>
  )
}